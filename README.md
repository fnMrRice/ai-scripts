# ai-scripts
scriptes generated by ai, for personal usage

### repack-archive.sh

该脚本用于将 `.gz` 压缩包重新打包为 `.xz` 格式。适用于需要将已有的 gzip 压缩包转换为 xz 格式以获得更高压缩率或满足特定需求的场景。

**用法示例：**
```bash
./repack-archive.sh file.tar.gz
```

**注意事项：**
- 请确保已安装 `xz` 命令。
- 脚本文件名以 `.sh` 结尾，表示其为 shell 脚本。
